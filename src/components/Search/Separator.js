import React, { Component } from "react";
import { FlatList, View, Text } from "react-native";

export default ({ items }) => (
  <View
    style={{
      height: 1,
      marginTop: 5,
      marginBottom: 5
    }}
  />
);
